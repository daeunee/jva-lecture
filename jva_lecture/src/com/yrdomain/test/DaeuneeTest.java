package com.yrdomain.test;

public class DaeuneeTest {
	public int loginProcess(String id, String pw,String email) {
		String[] db = {"daeunee", "0000","daeunee@naver.com"};
		
		
		int result = 0;
		  result = login(daeunee,0000,daeunee@naver.com);
		if (id == db[0] && pw == db[1] ) {
			result = 1;
		} else {
			result = 2;
		}
		return result; // 반환형 변수 또는 값
	}
input int 1(int로 받은 값을 출력하는 함수 ) ,outx
	public int loginProcess(String id, String pw) {
		String[] db = {"daeunee", "0000"};
		int result = 0;
		result = loginProcess("daeunee","0000");
		if (id == db[0] && pw == db[1] ) {
			result = 1;
		} else {
			result = 0;
		}
		return result; // 반환형 변수 또는 값
	}


// 안에는 syso input int 1(int로 받은 값을 출력하는 함수 ) ,outx
public void syso() {
 
 System.out.println();

public void syso (resultScores);

Scores.dddd(resultScores);

public int FFF (String daeun) {
	
	
	 return=7558;
	}

}
}
// 함수이름 자유, input: 본인이름 , output:핸드폰뒷자리4개
public int FFF (String daeun) {
	 return=7558;
	}

//STRING변수 명은 name (초기값으로 이름)

public int Name (String daeun) {
	
	int daeun = 0;
	
	System.out.println(name);

public void func(int a){
System.out.println(a);
}
	
String name = "김다은";
int resultNum = 0;
resultNum = login.getPhoneNumber(name);
login
name = phone(a);
syso(name);



public int Name(String daeun) {
	String[] db = {"daeun"};
	int result = 0;
	result = loginProcess("daeunee","0000");
	if (id == db[0] && pw == db[1] ) {
		result = 1;













	
}